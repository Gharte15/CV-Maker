import { AppBar, Typography } from "@mui/material"

export const Header = () => {
  return (
    <AppBar position="relative">
      <Typography variant="h6">CV-Creator</Typography>
    </AppBar>
  )
}